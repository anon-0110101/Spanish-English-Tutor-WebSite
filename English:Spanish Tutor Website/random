// Get all elements with the class "scroll-text"
const scrollTextElements = document.querySelectorAll('.scroll-text');

// Loop through each element
scrollTextElements.forEach(element => {
  // Add an event listener for the "scroll" event
  window.addEventListener('scroll', () => {
    // Get the bounding rectangle of the element
    const bounding = element.getBoundingClientRect();
    // Check if the element is in the viewport
    if (
      bounding.top >= 0 &&
      bounding.bottom <= (window.innerHeight || document.documentElement.clientHeight)
    ) {
      // Add the "show" class to the element
      element.classList.add('show');
    } else {
      // Remove the "show" class from the element
      element.classList.remove('show');
    }
  });
});
const logoButton = document.querySelector('#logo-button');

logoButton.addEventListener('click', () => {
  window.scrollTo({
    top: 0,
    behavior: 'smooth'
  });
});

const logo1Button = document.querySelector('#logo1-button');

logoButton.addEventListener('click', () => {
  window.scrollTo({
    top: 0,
    behavior: 'smooth'
  });
});

const logo2Button = document.querySelector('#logo2-button');

logoButton.addEventListener('click', () => {
  window.scrollTo({
    top: 0,
    behavior: 'smooth'
  });
});

const logo3Button = document.querySelector('#logo3-button');

logoButton.addEventListener('click', () => {
  window.scrollTo({
    top: 0,
    behavior: 'smooth'
  });
});

const logo4Button = document.querySelector('#logo4-button');

logoButton.addEventListener('click', () => {
  window.scrollTo({
    top: 0,
    behavior: 'smooth'
  });
});

const logo5Button = document.querySelector('#logo5-button');

logoButton.addEventListener('click', () => {
  window.scrollTo({
    top: 0,
    behavior: 'smooth'
  });
});

